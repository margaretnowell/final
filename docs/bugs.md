# DailyVibes Known Bugs and Limitations

- The application does not include user authentication. All entries are stored in a single shared table.
- Only the 50 most recent entries are returned by the API.
- Client-side form validation is minimal.
- Styling may vary slightly on older browsers.
- Charts update when data is refreshed, not in real time.
